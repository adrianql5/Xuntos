# Configuración da base de datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=xuntos_db
DB_USER=xuntos_user
DB_PASSWORD=xuntos_password

# Configuración do servidor
PORT=3000
JWT_SECRET=cambia_esto_por_unha_clave_secreta_moi_longa_e_aleatoria

# Ambiente (development, production)
NODE_ENV=development
